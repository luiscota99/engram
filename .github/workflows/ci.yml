name: Engram CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e '.[dev]'
    
    - name: Dependency smoke test (SQLite extensions)
      run: |
        python -c "import sqlean; import sqlite_vec; print('SQLite extensions loaded successfully')"

    - name: Lint with ruff
      run: |
        python -m ruff check src/ tests/ benchmarks/

    - name: Typecheck with pyright
      env:
        PYTHONPATH: .
      run: |
        python -m pyright
        
    - name: Test with pytest (with coverage report)
      env:
        PYTHONPATH: .
      run: |
        python -m pytest tests/ -v --cov=src --cov-report=term-missing

    - name: Retrieval benchmark smoke (seeded DB)
      env:
        ENGRAM_DB_PATH: ${{ runner.temp }}/engram_bench_ci.db
        PYTHONPATH: .
      run: |
        python benchmarks/engram_retrieval_bench.py --limit 5
