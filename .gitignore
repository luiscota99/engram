.venv/
__pycache__/
*.pyc
*.pyo
.env
data/*.db
data/*.db-journal
data/*.db-wal
# Local stress-test / temp DBs (do not commit; reproduce via inject_noise.py)
benchmarks/*.db
benchmarks/*.db-journal
benchmarks/*.db-wal
# Generated benchmark run output (results_baseline.json stays tracked)
benchmarks/longmemeval_results.json
stress_*.db
.DS_Store
*.egg-info/
dist/
build/
.coverage
htmlcov/

# Per-developer Claude Code settings — generated locally by `engram bootstrap`
# (recall + guard hooks). Not shipped, so contributors aren't forced to run
# hooks that assume engram is installed.
.claude/
